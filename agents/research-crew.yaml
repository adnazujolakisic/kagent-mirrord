apiVersion: kagent.dev/v1alpha2
kind: Agent
metadata:
  name: research-crew
  namespace: kagent
spec:
  type: BYO
  description: "A CrewAI research agent (LLM-only, no extra API keys) that summarizes findings"
  byo:
    deployment:
      image: localhost:5001/research-crew:latest
      env:
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: research-crew-secrets
              key: OPENAI_API_KEY
